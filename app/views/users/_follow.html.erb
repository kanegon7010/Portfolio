<%= form_with(model: current_user.following_relationships.build) do |f| %>
  <%= f.hidden_field :following_id, value: user.id %>
  <object>
  <%= f.submit "フォローする", 
    class: "relative z-10 text-center base_button bg-white text-blue-500 border-blue-500" %>
  </object>
<% end %>